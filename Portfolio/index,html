<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Fedda AlAhmed | Creative Portfolio</title>
    <style>
        body { margin: 0; font-family: 'Arial', sans-serif; background: #1a0033; color: white; }
        
        /* 1. Fixed Navbar */
        .navbar {
            position: fixed; top: 0; width: 100%; background: #4b0082; 
            display: flex; justify-content: center; z-index: 1000;
        }
        .navbar a { 
            color: #ff00ff; padding: 20px; text-decoration: none; font-weight: bold; 
        }
        .navbar a:hover { background: #ff00ff; color: white; transition: 0.3s; }

        /* 2. Video with Text Overlay */
        .hero-video { position: relative; height: 500px; overflow: hidden; margin-top: 60px; }
        video { width: 100%; height: 100%; object-fit: cover; }
        .overlay-text { 
            position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); 
            text-align: center; background: rgba(0,0,0,0.5); padding: 20px;
        }

        /* 3. Centered Image, Filter, Rounded Edges */
        .img-section { text-align: center; padding: 50px; }
        .profile-img { 
            width: 300px; border-radius: 80px; /* Rounded */
            filter: sepia(60%); /* CSS Filter */
        }

        /* 4. Two-Column Layout */
        .layout { display: flex; justify-content: center; gap: 20px; padding: 50px; }
        .col { 
            width: 400px; height: 200px; background: #2a0052; 
            color: #ffccff; padding: 20px; border: 1px solid #ff00ff; 
        }

        /* 5. Styled Table */
        table { width: 80%; margin: 20px auto; border: 3px solid #ff00ff; border-collapse: collapse; }
        td, th { padding: 15px; border: 1px solid #4b0082; }
        tr:first-child { background-color: #ff00ff; } /* Top row color */

        /* 6. Hover Effect for P */
        p.interact:hover { color: #00ffff; transform: scale(1.02); cursor: pointer; }

        /* 7. Image Overlay Fade */
        .fade-container { position: relative; width: 300px; margin: 0 auto; }
        .fade-overlay {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(255, 0, 255, 0.7); opacity: 0; transition: 0.5s;
            display: flex; align-items: center; justify-content: center;
        }
        .fade-container:hover .fade-overlay { opacity: 1; }

        footer { text-align: center; font-weight: bold; padding: 40px; background: #100020; }
    </style>
</head>
<body>
    <nav class="navbar">
        <a href="#home">Home</a><a href="#about">About</a><a href="#home">Home</a>
    </nav>

    <div class="hero-video">
        <video autoplay muted loop>
            <source src="https://assets.mixkit.co/videos/preview/mixkit-abstract-movement-of-pink-and-purple-ink-31435-large.mp4" type="video/mp4">
        </video>
        <div class="overlay-text"><h1>Fedda AlAhmed</h1><p>Digital Creator</p></div>
    </div>

    <div class="img-section">
        <img src="https://images.pexels.com/photos/1181673/pexels-photo-1181673.jpeg?auto=compress&cs=tinysrgb&w=400" class="profile-img">
    </div>

    <div class="layout">
        <div class="col"><h3>Design Philosophy</h3><p class="interact">I believe in bold colors and clean code.</p></div>
        <div class="col"><h3>Technical Skills</h3><p class="interact">Mastering Bootstrap and CSS Animations.</p></div>
    </div>

    <div class="fade-container">
        <img src="https://images.pexels.com/photos/1779487/pexels-photo-1779487.jpeg?auto=compress&cs=tinysrgb&w=300" style="width:100%">
        <div class="fade-overlay">View Project</div>
    </div>

    <table>
        <tr><th>Project</th><th>Year</th><th>Status</th></tr>
        <tr><td>Cinema Web</td><td>2026</td><td>Completed</td></tr>
    </table>

    <footer>FEDDA ALAHMED © 2026</footer>
</body>
</html>
